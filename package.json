{
  "name": "mashiltax",
  "version": "1.0.0",
  "private": true,
  "description": "MashilTax — Simple personal tax assistant for Malaysia",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
